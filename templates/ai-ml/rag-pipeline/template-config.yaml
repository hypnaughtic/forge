name: rag-pipeline
description: RAG system with vector DB, embeddings, retrieval, and llm-gateway generation
category: ai-ml
languages:
  - python
frameworks:
  - llm-gateway
  - chromadb
tags:
  - rag
  - vector-search
  - embeddings
  - llm
  - llm-gateway
  - retrieval
min_python_version: "3.11"
min_mode: mvp
scaffold_dir: scaffold
post_create_commands:
  - "python -m venv .venv"
  - "pip install -r requirements.txt"
  - "cp .env.example .env"
