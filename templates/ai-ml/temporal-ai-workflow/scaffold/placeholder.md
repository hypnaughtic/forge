# Scaffold Placeholder

> This is a stub. No scaffold files have been generated yet.

A full scaffold would include:
- `workflows/` -- Temporal workflow definitions (pipeline orchestration)
- `activities/` -- activity implementations wrapping llm-gateway
- `workers/` -- Temporal worker configuration and startup
- `gateway/` -- llm-gateway client configuration
- `tests/` -- workflow replay tests with mocked activities
- `docker-compose.yaml` -- Temporal server, workers, llm-gateway
