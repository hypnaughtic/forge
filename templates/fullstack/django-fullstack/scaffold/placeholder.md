# Scaffold Placeholder

> This is a stub. No scaffold files have been generated yet.

A full scaffold would include:
- `config/` -- Django project settings split by environment
- `apps/` -- Django application modules (users, core, etc.)
- `apps/*/serializers.py` -- DRF serializers per app
- `apps/*/tasks.py` -- Celery task definitions per app
- `templates/` -- Django HTML templates
- `docker-compose.yaml` -- full stack (web, worker, beat, db, redis)
- `requirements/` -- pip requirements split by environment
