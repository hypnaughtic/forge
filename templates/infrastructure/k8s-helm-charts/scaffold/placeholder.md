# Scaffold Placeholder

> This is a stub. No scaffold files have been generated yet.

A full scaffold would include:
- `charts/app/` -- application Helm chart
- `charts/library/` -- shared library chart with reusable templates
- `environments/` -- per-environment values files
- `argocd/` -- ArgoCD Application manifests (or Flux HelmRelease)
- `tests/` -- helm-unittest test definitions
- `ct.yaml` -- chart-testing configuration
