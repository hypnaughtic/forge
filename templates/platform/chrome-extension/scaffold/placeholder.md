# Scaffold Placeholder

> This is a stub. No scaffold files have been generated yet.

A full scaffold would include:
- `src/background/` -- service worker event handlers
- `src/content/` -- content scripts for page interaction
- `src/popup/` -- popup UI components
- `src/options/` -- options page for settings
- `src/shared/` -- shared types and message definitions
- `manifest.json` -- Manifest V3 configuration
- `vite.config.ts` -- build configuration with CRXJS plugin
