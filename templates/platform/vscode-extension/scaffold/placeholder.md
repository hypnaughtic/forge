# Scaffold Placeholder

> This is a stub. No scaffold files have been generated yet.

A full scaffold would include:
- `src/extension.ts` -- extension entry point with activation
- `src/server/` -- LSP server implementation
- `src/providers/` -- completion, diagnostic, and code action providers
- `src/webview/` -- webview panel UI components
- `package.json` -- extension manifest with contributions
- `.vscode/launch.json` -- debug configurations
- `test/` -- integration tests with test runner
