#!/usr/bin/env bash
# Mock docker — version check, exits 0
echo "docker $*" >> "${MOCK_LOG_DIR:-/tmp}/docker.log" 2>/dev/null || true

case "${1:-}" in
    --version)
        echo "Docker version 24.0.0, build mock"
        ;;
    *)
        exit 0
        ;;
esac
