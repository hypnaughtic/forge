---
name: cost
description: "Detailed cost breakdown by agent with budget tracking"
argument-hint: ""
---

# /forge:cost — Cost Breakdown

Run the cost tracker and display a detailed breakdown:

```bash
bash "$FORGE_DIR/scripts/cost-tracker.sh" --report
```

Display the output to the user. If the budget is approaching the cap, highlight the warning.

$ARGUMENTS
