# /forge-team — Team View

Show detailed per-agent information.

Run:

```bash
bash $FORGE_DIR/scripts/team-view.sh $ARGUMENTS
```

Without arguments: shows all agents with status, current task, and cost.
With agent name: shows a deep dive including working memory, decisions, and artifacts.

Usage: `/forge-team`, `/forge-team backend-developer`

$ARGUMENTS
