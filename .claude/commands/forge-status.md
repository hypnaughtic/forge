# /forge-status — Project Status

Report from your working memory:

1. Iteration number, phase, task progress (done/total)
2. Active agents and their current tasks
3. Any blockers

Then run `bash $FORGE_DIR/scripts/cost-tracker.sh --report` for cost summary.

Keep it brief. The user wants a quick snapshot, not a deep analysis.

$ARGUMENTS: none
